import React from 'react';
import { UserList } from '../components/UserList';

export const UserListPage: React.FC = () => {
    return (
        <div>
            <h1>User List</h1>
            <UserList />
        </div>
    );
};
